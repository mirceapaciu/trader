# TradeExecutor production configuration.
# Loaded after .env.shared / .env.prod.

TRADE_EXECUTOR_DB_SCHEMA=trade_executor
SHARED_DB_SCHEMA=shared
MARKET_DATA_DB_SCHEMA=market_data

# Trading parameters
WATCHLIST=AAPL,MSFT,GOOGL,AMZN,TSLA,NVDA,META
MAX_POSITION_SIZE=3000
MAX_DAILY_TRADES=10
MAX_PORTFOLIO_EXPOSURE=10000
DAILY_LOSS_LIMIT=200

# IBKR host/port/client-id/trading-mode live in .env.shared so all components agree.
