# Optional Backtester production configuration.
# Loaded after .env.shared / .env.prod when present.

BACKTESTER_DB_SCHEMA=backtester
SHARED_DB_SCHEMA=shared
THESIS_BUILDER_DB_SCHEMA=thesis_builder
MARKET_DATA_DB_SCHEMA=market_data
