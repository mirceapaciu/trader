# Used by the Postgres image when the database volume is first initialized.
# Changing these after the volume exists will not recreate or rename the DB.
POSTGRES_DB=trader
POSTGRES_USER=trader
POSTGRES_PASSWORD=<REQUIRED_POSTGRES_PASSWORD>
