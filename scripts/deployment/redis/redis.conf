bind 0.0.0.0
port 6379

# Persistence for durable queue semantics
appendonly yes
appendfsync everysec

# Disable RDB snapshots for local simplicity; AOF is primary durability mode
save ""

# Local development defaults
protected-mode yes
