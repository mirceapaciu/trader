# Production overrides.
# Load after .env.shared so these values override shared defaults.

POSTGRES_DATABASE=trader
POSTGRES_PROD_DATABASE=trader

# Use production credentials through secure secret management.
# POSTGRES_USER=trader_prod
# POSTGRES_PASSWORD=<set-in-secret-store>

PAPER_TRADING=false
