# verify-story-assignment — pass 260716, assignments 157–173

**First pass after the 260716-01 fix** (`fd4f7c6`) deployed and thesis-builder restarted at
11:58. These 17 assignments were produced by the three-band verifier (deterministic 0 / ≥2
bands + LLM same-event check on the single-token band). This pass exists to validate the fix
live.

## Headline

The event-check fires exactly where designed and correctly. **4 of 6 downgrades are the new
`story_event_mismatch` path** — single-token cross-story matches the pre-fix ≥1 rule would have
passed. No false downgrades, no new ≥2-token residual false-negatives. The fix works on live
data.

## Counts

- **Audited:** 17 (15 `matched` / 2 `new_story` / 0 `fallback`)
- **Verifier:** 6 `downgraded` (4 via event-check, 2 via zero-overlap), 9 `passed` (all ≥2 overlap)
- **My judgment:** all 15 matched decisions correct; 0 over-correction, 0 residual.

## Event-check catches (the fix, live) — new corpus anchors CS-0067..CS-0070

| assn | ticker | LLM matched → window | overlap | actual event |
|---|---|---|---|---|
| 163 | MU | Micron US-investment | `memory` | a late-2027 price prediction |
| 165 | MU | Micron US-investment | `positive` | a Zacks earnings-trends roundup |
| 166 | MU | "Bull of the Day: Micron" | `bull` | a multi-stock Zacks Bull list |
| 167 | NVDA | NVIDIA-China-clients | `potential` | a Fed/Warsh macro article |

Each shared exactly one token with its target window; the LLM confirmed "different event" and the
article was downgraded to `new_story` (`verification_status='downgraded'`,
`reason_code='story_event_mismatch'`, `details.event_check='different'`). #165 is the same
`positive` single-token shape that published the cross-story MSFT card last pass (CS-0061) — now
caught. These are frozen as GREEN regression anchors: the same inputs must keep downgrading.

## Deterministic bands (unchanged behaviour, all correct)

- **Zero-overlap downgrades (2):** #161 (Meta rating-upgrade vs the buy-thesis window) and #164
  (Micron "Bull of the Day" vs the US-investment window). Correct.
- **≥2-overlap passes (9):** all genuine same-story — GameStop/Uber-Eats retail expansion
  (158, 169), Uber–Delivery-Hero M&A onto its card (159, 170, 171, 172, 173), MSFT
  earnings-prediction (162), Japan-robotics (168). No LLM call needed; no residual slipped.

## Consequences

- 4 cards currently published (`03a7571a` Japan, `69a2ad06` Uber DH, `76df158d` MSFT, `b873d0cd`
  NVDA). The MSFT one is the pre-fix cross-story card (CS-0061), unchanged. No *new* cross-story
  card this pass.

## Corpus / issues

- Appended **4 rows** CS-0067..CS-0070 (corpus now **70**): the live event-check catches, as
  post-fix green anchors (new fields `verification_reason_code`, `overlap`, `event_check`).
- No issue changes. 260716-01 remains resolved and is now demonstrated working live.

## verify-story-assignment: ids 157–173

**Audited:** 17 (15 matched / 2 new_story) — first post-fix batch
**Verifier:** 6 downgraded (4 event-check `story_event_mismatch`, 2 zero-overlap), 9 passed (all ≥2, correct)
**Errors:** 0 residual, 0 over-correction — the 4 event-check downgrades are correct catches, frozen green
**Corpus:** +4 rows (now 70)
**Issues:** none (260716-01 validated working live)
**Report:** docs/verification-runs/story-assignment/260716-157-173.md
