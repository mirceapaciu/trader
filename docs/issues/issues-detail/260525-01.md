# Feature: Implement core event-ingestion-engine asset

## Problem Statement

The repository has the design specification for the Real-time Event Ingestion and Signal Preprocessing Engine but no executable Python implementation of the core asset contracts.

## Verified Evidence

- Design specification exists at docs/design/core-components/event-ingestion-engine/event-ingestion-engine.md.
- No Python files existed under src/ before implementation.

## Expected Behavior

Provide a reusable Python implementation of the event-ingestion-engine with:

- canonicalization and deterministic id generation,
- strong and soft deduplication,
- persist -> publish -> checkpoint orchestration,
- adapter-based interfaces for source, storage, and publication,
- tests that validate core contract behavior.

## Acceptance Criteria

- Canonical events follow schema and deterministic id contract.
- Soft dedupe default algorithm (weighted_text_locator_v1) is implemented with deterministic behavior.
- Engine checkpoint advancement is gated by terminal publication obligations.
- Implementation remains reusable and component-agnostic.
- Unit tests cover canonicalization, dedupe, and orchestration paths.

## Test Plan

- Run: uv run pytest -m "not integration"
- Focused test path: tests/core_components/event_ingestion_engine

## Root Cause

N/A (feature implementation).

## Resolution Steps

- Added reusable core module implementation under src/core_components/event_ingestion_engine with:
  - canonical event models and adapter protocols,
  - deterministic canonicalization and id generation,
  - strong and soft dedupe engine,
  - persist -> publish -> checkpoint orchestration.
- Added focused unit tests under tests/core_components/event_ingestion_engine for canonicalization, dedupe decisions, and checkpoint gate behavior.
- Attempted to run focused tests, but execution environment lacks pytest and cannot install it system-wide due permission restrictions.

## Remaining Risks / Follow-up

- Integrate with concrete repository and broker adapters in product components.
- Add integration tests against PostgreSQL and Redis broker.
- Install pytest in a project-local virtual environment and run the focused test suite.
